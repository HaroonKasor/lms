services:
  db:
    image: mysql:8.4
    container_name: lms-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-lms_enterprise}
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -p$$MYSQL_ROOT_PASSWORD"]
      interval: 10s
      timeout: 5s
      retries: 12

  web:
    build:
      context: ./next-lms
      dockerfile: Dockerfile
    container_name: lms-web
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV: production
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_BASE_URL: ${OPENROUTER_BASE_URL:-https://openrouter.ai/api/v1}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-openai/gpt-4o-mini}
      GROQ_API_KEY: ${GROQ_API_KEY:-}
      GROQ_BASE_URL: ${GROQ_BASE_URL:-https://api.groq.com/openai/v1}
      GROQ_MODEL: ${GROQ_MODEL:-llama-3.1-8b-instant}
      SESSION_SECRET: ${SESSION_SECRET}
      SESSION_COOKIE_SECURE: ${SESSION_COOKIE_SECURE:-true}
      APP_NAME: ${APP_NAME:-SkillUp}
      APP_URL: ${APP_URL:-https://${APP_DOMAIN}}
      CRON_SECRET: ${CRON_SECRET:-}
      PASSWORD_RESET_BASE_URL: ${PASSWORD_RESET_BASE_URL:-}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_SECURE: ${SMTP_SECURE:-false}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASS: ${SMTP_PASS:-}
      SMTP_FROM: ${SMTP_FROM:-}
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      RESEND_FROM: ${RESEND_FROM:-}
      RESEND_BASE_URL: ${RESEND_BASE_URL:-https://api.resend.com}
      ALLOW_AUTH_SEED: ${ALLOW_AUTH_SEED:-false}
      AUTH_SEED_TOKEN: ${AUTH_SEED_TOKEN:-}
      NEXT_PUBLIC_APP_URL: https://${APP_DOMAIN}
      NEXT_PUBLIC_XAPI_OBJECT_BASE_URL: https://${APP_DOMAIN}
      DB_HOST: db
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      DB_NAME: ${MYSQL_DATABASE:-lms_enterprise}
      DATABASE_URL: mysql://root:${MYSQL_ROOT_PASSWORD}@db:3306/${MYSQL_DATABASE:-lms_enterprise}
    volumes:
      - ./next-lms/data:/app/data
      - ./next-lms/public/content:/app/public/content
      - ./next-lms/public/uploads/courses:/app/public/uploads/courses

  cron-reminder:
    image: curlimages/curl:8.8.0
    container_name: lms-cron-reminder
    restart: unless-stopped
    depends_on:
      - web
    environment:
      CRON_SECRET: ${CRON_SECRET:-}
      ENROLLMENT_REMINDER_CRON_INTERVAL_SECONDS: ${ENROLLMENT_REMINDER_CRON_INTERVAL_SECONDS:-3600}
    command: >
      sh -c 'while true; do
        if [ -n "$$CRON_SECRET" ]; then
          curl -fsS -H "Authorization: Bearer $$CRON_SECRET" "http://web:3000/api/cron/enrollment-expiry-reminders" || true;
        fi;
        sleep "$${ENROLLMENT_REMINDER_CRON_INTERVAL_SECONDS:-3600}";
      done'

  phpmyadmin:
    image: phpmyadmin:5-apache
    container_name: lms-phpmyadmin
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_AUTH_TYPE: cookie
      PMA_ABSOLUTE_URI: https://${APP_DOMAIN}/phpmyadmin/
      UPLOAD_LIMIT: 256M

  caddy:
    image: caddy:2.8
    container_name: lms-caddy
    restart: unless-stopped
    depends_on:
      - web
      - phpmyadmin
    environment:
      APP_DOMAIN: ${APP_DOMAIN}
      LETSENCRYPT_EMAIL: ${LETSENCRYPT_EMAIL}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

volumes:
  mysql_data:
  caddy_data:
  caddy_config:

